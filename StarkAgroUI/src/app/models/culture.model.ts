// Cultura da lista canônica da plataforma (gerida pelo admin). Alimenta os seletores de
// cultura em área, perfil de adubação e diagnóstico.
export interface Culture {
  id: number;
  name: string;
}
