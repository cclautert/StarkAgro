# Copy to docker/.env and fill in real values.
# NEVER commit .env with real secrets.

# JWT
JWT_SECRET_KEY=CHANGE_ME_min_32_chars_random_string
JWT_ISSUER=starkagro.com.br
JWT_AUDIENCE=https://starkagro.com.br

# Google OAuth (https://console.cloud.google.com/ → APIs & Services → Credentials)
GOOGLE_CLIENT_ID=CHANGE_ME
GOOGLE_CLIENT_SECRET=CHANGE_ME

# MongoDB auth (must match MONGO_INITDB_ROOT_* in docker-compose.yml)
MONGO_USER=agripeweb_admin
MONGO_PASSWORD=CHANGE_ME_strong_password

# MQTT (internal broker — see docker/mosquitto/passwd.example)
MQTT_USERNAME=iot_device
MQTT_PASSWORD=CHANGE_ME

# Google Gemini API (optional — required for "Analisar com IA" feature)
# Free tier: 1500 requests/day — https://aistudio.google.com/apikey
GEMINI_API_KEY=CHANGE_ME

# Web Push / VAPID (required for browser push notifications)
# Generate once: npx web-push generate-vapid-keys --json
# VAPID_PUBLIC_KEY is also set in appsettings.json and environment.prod.ts
VAPID_SUBJECT=mailto:admin@starkagro.com.br
VAPID_PUBLIC_KEY=CHANGE_ME
VAPID_PRIVATE_KEY=CHANGE_ME
