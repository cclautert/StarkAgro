# Mosquitto — StarkAgro VPS. Só listener interno 1883 (sem TLS/8883: os certs
# ficam para quando houver IoT externo). Auth obrigatória via password_file.
listener 1883
allow_anonymous false
password_file /mosquitto/config/passwd
persistence true
persistence_location /mosquitto/data/
log_dest stdout
